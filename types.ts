// Interfaces are removed as they are a TypeScript-only feature.
// Enums are converted to plain JavaScript objects for browser compatibility.
export const ChatRole = {
    USER: 'user',
    MODEL: 'model',
};
